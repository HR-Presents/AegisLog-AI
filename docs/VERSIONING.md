# Versioning

AegisLog AI is pre-1.0. Minor releases may change command details and report schemas while the architecture is validated. Stable V1.0 should adopt semantic versioning guarantees for documented CLI behavior, configuration, and machine-readable output.
