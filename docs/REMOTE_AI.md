# Future remote AI boundary

Remote AI is not enabled in V0.2. A future implementation should require explicit configuration, obtain credentials outside the normal config file, show whether analysis leaves the machine, minimize event context, redact recognized secrets, bound request sizes, and fall back cleanly to local analysis.
